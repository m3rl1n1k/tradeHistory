{% extends 'base.html.twig' %}

{% block title %}{{ 'edit.title'|trans({},'buget') }}{% endblock %}

{% block body %}
    <h2 class="text-gray-900 dark:text-gray-300 text-2xl">{{ 'edit.title'|trans({},'buget') }}</h2>

    {{ include('budget/_form.html.twig', {'button_label': 'edit.update'|trans({},'buget')}) }}
{% endblock %}