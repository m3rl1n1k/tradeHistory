{% extends 'base.html.twig' %}

{% block title %}{{ 'new.title'|trans({}, 'buget') }}{% endblock %}

{% block body %}
    <h2 class="text-gray-900 dark:text-gray-300 text-2xl">{{ 'new.title'|trans({}, 'buget') }}</h2>

    {{ include('budget/_form.html.twig') }}
{% endblock %}