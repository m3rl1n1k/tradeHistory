<form method="post" action="{{ path('app_transfer_delete', {'id': transfer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfer.id) }}">
    <button class="btn">Delete</button>
</form>
