{{ form_start(form) }}
{{ include('component/_alert.html.twig') }}
<div class="mb-6">
    <div class="lg:w-1/2 md:w-full">
        <div class="flex mb-3">
            <span class="inline-flex items-center px-3 py-2 text-gray-500 border border-r-0 border-gray-300 rounded-l-md">
                {{ form_label(form.name, 'Name', {'label_attr': {'class': 'form-label'}}) }}
            </span>
            {{ form_widget(form.name, {'attr': {'class': 'form-input w-full border-gray-300 rounded-md'}}) }}

            {% if app.user.setting.coloredParentCategories %}
                <span class="inline-flex items-center px-3 py-2 text-gray-500 border border-l-0 border-gray-300 rounded-r-md">
                    {{ form_widget(form.color, {'attr': {'class': 'form-control form-control-color'}}) }}
                </span>
            {% endif %}
        </div>
        {{ include('component/_button.html.twig', {
            'href': path('app_category_new'),
            'button_label': 'Save'
        }) }}
    </div>
</div>

{{ include('component/_link.html.twig', {
    'href': path('app_parent_category_index'),
    'button_label': 'Save'
}) }}

{{ form_end(form) }}

