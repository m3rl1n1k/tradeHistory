{{ form_start(form, {'attr': {'class': 'space-y-6 bg-white dark:bg-gray-700 p-6 rounded-lg shadow-lg'}}) }}
{{ include('component/_alert.html.twig') }}

<div class="space-y-6">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="mt-3">
            <label for="{{ form.wallet.vars.id }}"
                   class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.from'|trans({}, 'transaction') }}</label>
            {{ form_widget(form.wallet, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                    'placeholder': 'form.from'|trans({}, 'transaction')
                }
            }) }}

        </div>

        <div class="mt-3">
            <label for="{{ form.amount.vars.id }}"
                   class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.amount'|trans({}, 'transaction') }}</label>
            {{ form_widget(form.amount, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                    'placeholder': 'form.amount'|trans({}, 'transaction')
                }
            }) }}
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-3">
        <div class="mt-3">
            <label for="{{ form.category.vars.id }}"
                   class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.category'|trans({}, 'transaction') }}</label>
            {{ form_widget(form.category, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                    'placeholder': 'form.category'|trans({}, 'transaction')
                }
            }) }}
        </div>

        <div class="mt-3">
            <label for="{{ form.type.vars.id }}"
                   class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.type'|trans({}, 'transaction') }}</label>
            {{ form_widget(form.type, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                    'placeholder': 'form.type'|trans({}, 'transaction')
                }
            }) }}
        </div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-3">
        <div class="mt-3">
            <label for="{{ form.date.vars.id }}"
                   class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.date'|trans({}, 'transaction') }}</label>
            {{ form_widget(form.date, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                    'max': form.date.vars.value
                }
            }) }}
        </div>
    </div>

    <div class="mt-3">
        <label for="{{ form.description.vars.id }}"
               class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'form.description'|trans({}, 'transaction') }}</label>
        {{ form_widget(form.description, {
            'attr': {
                'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent',
                'placeholder': 'form.description'|trans({}, 'transaction')
            }
        }) }}
    </div>

    <div class="mt-4 flex justify-between items-center">
        <button class="bg-green-500 text-white py-2 px-4 rounded-md hover:bg-green-600">{{ button_label|default( 'form.save'|trans({}, 'transaction') ) }}</button>
        {{ include('component/_link.html.twig') }}
    </div>
</div>

{{ form_end(form) }}