<form method="post" action="{{ path('app_category_delete', {'id': category.id}) }}"
      onsubmit="return confirm({{ 'confirm_message'|trans({}, 'base') }});">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="bg-red-500 text-white px-3 py-1 rounded text-sm hover:bg-red-600">{{ 'delete'|trans({}, 'category') }}</button>
</form>