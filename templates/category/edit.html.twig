{% extends 'base.html.twig' %}

{% block title %}{{ 'edit.title'|trans({},'category') }}{% endblock %}

{% block body %}
    <h2 class="text-gray-900 dark:text-gray-300 text-2xl">{{ 'edit.title'|trans({},'category') }}</h2>

    {{ include('category/_form.html.twig', {'button_label': 'edit.update'|trans({}, 'category')}) }}



{% endblock %}