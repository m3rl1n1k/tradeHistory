{% extends 'base.html.twig' %}

{% block title %}Hello {{ app.user.email }}{% endblock %}

{% block body %}

    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript" src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>

    <div class="example-wrapper">
{#    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">#}
{#        <h1 class="h2">Dashboard</h1>#}
{#        <div class="btn-toolbar mb-2 mb-md-0">#}
{#            <div class="btn-group me-2">#}
{#                <button type="button" class="btn btn-sm btn-outline-secondary">Export</button>#}
{#            </div>#}
{#            <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">#}
{#                <span data-feather="calendar"></span>#}
{#                This week#}
{#            </button>#}
{#        </div>#}
{#    </div>#}

{#        <div class="my-4 w-100" id="donutchart" style="width: 900px; height: 500px;"></div>#}

    <h2>Last 10 transactions</h2>
    <table class="table table-hover">
        <thead>
        <tr>
            <th>#</th>
            <th>Type</th>
            <th>Amount</th>
            <th>Category</th>
            <th>Date</th>
        </tr>
        </thead>
        <tbody>
        {% for  transaction in last10transaction %}
            <tr>
                <td scope="row">{{ loop.index }}</td>
                <td>{{ type(transaction.type) }}</td>
                <td class="text-center">
                    <a class="btn btn-outline-info text-decoration-none"
                       href="{{ path('app_transaction_show', {'id': transaction.id}) }}">{{ transaction.amount }}</a>
                </td>
                <td>{{ transaction.category ? transaction.category.name : '' }}</td>
                <td>{{ transaction.date ? transaction.date|date('Y-m-d') : '' }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <script>
        {#const Transaction = {{ transactions|json_encode|raw }};#}
    </script>
{% endblock %}