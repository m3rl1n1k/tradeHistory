{% extends 'base.html.twig' %}

{% block title %}{{ 'edit.title'|trans({},'wallet') }}{% endblock %}

{% block body %}
    <h2 class="text-gray-900 dark:text-gray-300 text-2xl">{{ 'edit.title'|trans({},'wallet') }}</h2>

    {{ include('wallet/_form.html.twig', {'button_label': 'edit.update'|trans({},'wallet')}) }}
{% endblock %}