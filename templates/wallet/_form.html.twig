{{ form_start(form, {'attr': {'class': 'space-y-6 bg-white dark:bg-gray-700 p-6 rounded-lg shadow-lg'}}) }}
{{ include('component/_alert.html.twig') }}

<div class="space-y-6">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div class="w-full sm:w-80">
            <label for="{{ form.currency.vars.id }}" class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'Currency' }}</label>
            {{ form_widget(form.currency, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent'
                }
            }) }}
        </div>
        <div class="mt-3">
            <label for="{{ form.name.vars.id }}" class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'Name of wallet' }}</label>
            {{ form_widget(form.name, {
                'attr': {
                    'class': 'w-full sm:w-80 p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent'
                }
            }) }}
        </div>
        <div class="w-full sm:w-80">
            <label for="{{ form.amount.vars.id }}" class="block text-gray-900 dark:text-gray-300 font-semibold mb-2">{{ 'Amount' }}</label>
            {{ form_widget(form.amount, {
                'attr': {
                    'class': 'w-full p-3 rounded-md border border-gray-300 dark:bg-gray-800 dark:border-gray-600 text-gray-900 dark:text-gray-200 focus:ring-2 focus:ring-blue-500 focus:border-transparent'
                }
            }) }}
        </div>
    </div>

    <div class="mt-3 flex items-center">
        <label for="isMain" class="form-check-label mr-2 text-gray-900 dark:text-gray-300 font-semibold">{{ 'Main wallet:' }}</label>
        {{ form_widget(form.isMain, {'attr': {'class': 'form-check-input'}}) }}
    </div>

    <div class="mt-4 flex justify-between items-center">
        {{ include('component/_button.html.twig') }}
        {{ include('component/_link.html.twig', {'href': path('app_wallet_index') }) }}
    </div>
</div>

{{ form_end(form) }}