{% extends 'base.html.twig' %}

{% block title %}Wallet{% endblock %}

{% block body %}
    <h1>Wallet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Number</th>
                <td>{{ wallet.number }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ wallet.name }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ wallet.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wallet_index') }}">back to list</a>

    <a href="{{ path('app_wallet_edit', {'number': wallet.number}) }}">edit</a>

    {{ include('wallet/_delete_form.html.twig') }}
{% endblock %}
